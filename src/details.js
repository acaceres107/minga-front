/* let id = Location.search.slice(4) */
/* let manga = mangas.find(manga => manga.id === id) */
/* 
let details = (unManga) => {
    return `
        <div class="details">
            <img src="${unManga.photo}">
            <p class="details-par center">${unManga.title} - ${unManga.author_id}</p>
            <p class="details-text center">${unManga.description}</p>
            <p class="details-par center">${unManga.category_id}</a>
        </div>
    `
}

document.querySelector('#details').innerHTML = details(manga) */